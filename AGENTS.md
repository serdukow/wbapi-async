# Claude Code Instructions

## What this project is

Async Python client for the Wildberries Seller API. Pydantic-free — responses
are plain `ApiResponse` objects with attribute access. No generated method
classes, no type models. Just HTTP verbs + path.

```python
async with WbAPI(token="...") as api:
    orders = await api.get("/api/v3/orders/new", limit=10, next=0)
    print(orders.orders[0].id)

    supplies = await api.get_all("/api/v3/supplies")  # auto-pagination

    await api.post("/adv/v0/rename", body={"advertId": 123, "name": "new"})
```

## File structure

```
src/wbapi_async/
├── __init__.py          # public exports: WbAPI, WBAPIError, TokenValidationError,
│                        #   PaginationNotSupported, ApiResponse
├── api.py               # WbAPI class — get/post/put/patch/delete/get_all
├── type.py              # ApiResponse — dict-like wrapper with attribute access
├── _method.py           # MethodDispatcher — dispatch, fetch_all, resolve_url
├── _registry.py         # _BASES, _LIMITS, _PUBLIC (AUTO-GENERATED — do not edit fmt: off block)
├── exceptions.py        # WBAPIError, TokenValidationError, PaginationNotSupported
├── utils/
│   ├── token.py         # validate_token()
│   ├── paginate.py      # pagination strategies
│   └── unofficial.py    # @unofficial decorator
└── client/
    └── session/
        ├── base.py      # BaseSession — httpx, rate limiting, 429 retry
        └── headers.py   # Headers plain class
```

## Auto-generated section in \_registry.py

`_BASES` and `_LIMITS` dicts are between `# fmt: off` and `# fmt: on` markers.
Regenerated by codegen from WB OpenAPI specs. **Do not edit that block manually.**

## Where to look up endpoints

Full docs: https://dev.wildberries.ru/en/docs/openapi/api-information

| File                                                                                              | Coverage                          |
| ------------------------------------------------------------------------------------------------- | --------------------------------- |
| [01-general.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/01-general.yaml)                 | warehouses, ping                  |
| [02-products.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/02-products.yaml)               | cards, stocks, prices, tags       |
| [03-orders-fbs.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/03-orders-fbs.yaml)           | orders, supplies, shipments (FBS) |
| [04-orders-dbw.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/04-orders-dbw.yaml)           | orders DBW                        |
| [05-orders-dbs.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/05-orders-dbs.yaml)           | orders DBS                        |
| [06-in-store-pickup.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/06-in-store-pickup.yaml) | in-store pickup                   |
| [07-orders-fbw.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/07-orders-fbw.yaml)           | orders FBW                        |
| [08-promotion.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/08-promotion.yaml)             | ad campaigns, budgets, bids       |
| [09-communications.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/09-communications.yaml)   | questions, reviews, news          |
| [10-tariffs.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/10-tariffs.yaml)                 | tariffs                           |
| [11-analytics.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/11-analytics.yaml)             | sales funnel, turnover, stocks    |
| [12-reports.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/12-reports.yaml)                 | detailed sales report             |
| [13-finances.yaml](https://dev.wildberries.ru/api/swagger/yaml/en/13-finances.yaml)               | payments, penalties               |

## Safe to edit

Everything except the `# fmt: off` block in `_registry.py`.

## Key design decisions

- **URL resolution**: `resolve_url(path)` does prefix lookup in `_BASES`.
  Dynamic paths like `/api/v3/supplies/WB-123/orders` resolve via parent prefix.
  Unknown path → `WBAPIError` with a link to `https://dev.wildberries.ru`.

- **Rate limiting**: `_LIMITS` maps path → `(period_ms, limit, interval_ms, burst)`.
  `MethodDispatcher` builds `AsyncLimiter` instances shared globally by `(interval_ms, burst)`.

- **Pagination** (`get_all`): auto-detects cursor (`next` field) or offset (full page = 1000).
  Custom paginator via `paginator=` kwarg: `callable(response) -> (items, next_params | None)`.

- **ApiResponse**: wraps raw dicts/lists recursively. `result.fieldName` works,
  `result.unwrap()` returns raw data, `result.to_snake()` returns dict with snake_case keys.

## Commands

```bash
uv sync --all-extras     # install deps
uv run pytest            # run tests
uv run ruff check src/   # lint
uv build --wheel         # build
```
