## What does this PR do?

<!-- Briefly describe the change: bug fix, new feature, refactor, etc. -->

## Checklist

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check src/` passes
- [ ] Hand-written methods under `methods/` (marked `__unofficial__ = True`) are not removed
- [ ] If adding a new method — a test in `tests/test_methods/` is included
