## What does this PR do?

<!-- Briefly describe the change: bug fix, new feature, refactor, etc. -->

## Checklist

- [ ] `uv run pytest` passes (coverage stays at 95% or above)
- [ ] `uv run ruff check src tests scripts` and `uv run ruff format --check src tests scripts` pass
- [ ] `uv run mypy` passes
- [ ] The section packages under `src/wbapi/` were not edited by hand — they are
      generated by `uv run python scripts/codegen.py`
- [ ] Behaviour changes come with a test

## Specs

<!-- Fill in only if specs/ changed. -->

- [ ] The client was regenerated and the diff reviewed
